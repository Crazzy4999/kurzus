export type addressInfo = {
    id: number
    userID: number
    isActive: boolean
    city: string,
    street: string,
    houseNumber: string,
    zipCode: string,
    floorNumber: string,
    apartment: string
}

export type productInfo = {
    count: number
    name: string
    price: number
}
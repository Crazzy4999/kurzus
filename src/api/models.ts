export type addressInfo = {
    city: string,
    street: string,
    houseNumber: string,
    zipCode: string,
    floorNumber: string,
    apartment: string
}

export type productInfo = {
    count: number
    name: string
    price: number
}